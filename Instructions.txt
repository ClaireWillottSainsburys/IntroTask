To run locally

Open terminal and enter the following command to run the server from the repository root:

cd "/Users/claire.willott/IntroTaskTemplate" && python3 -m http.server 8000

Then open your browser and go to: http://localhost:8000/index.html

Note: Files are now in the repository root for GitHub Pages hosting.